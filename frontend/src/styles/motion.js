// Motion tokens (Phase 7). Values are copied as-is from the framer-motion
// props already in use, so importing these changes nothing visually — it
// only gives the repeated/duplicated numbers one shared source.

export const EASE_OUT = "easeOut";
export const EASE_IN_OUT = "easeInOut";

export const DURATION_BASE = 0.6;
export const DURATION_FAST = 0.35;
export const DURATION_PULSE = 2;

export const ICON_CYCLE_INTERVAL_MS = 1600;